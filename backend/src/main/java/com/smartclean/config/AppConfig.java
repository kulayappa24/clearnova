package com.smartclean.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Add miscellanous beans here if required (e.g. RestTemplate, ObjectMapper config)
}
